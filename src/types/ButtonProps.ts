type ButtonProps = {
    bgColor?: string,
    bgColorOn?: string,
    borderColor?: string,
    borderColorOn?: number | string,
    textColor?: string,
    textColorOn?: string,
    w: number | string,
    h: number | string,
    borderR?: string,
    center?: boolean,
};

export default ButtonProps