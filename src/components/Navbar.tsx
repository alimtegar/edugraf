const Navbar = () => {
    return (
        <nav className="flex justify-center items-center bg-black text-white h-16">
            <a className="font-extrabold text-lg" href="#">Sibisa</a>
        </nav>
    );
};

export default Navbar;